<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ez.en.page.userMapper">
	<!-- 로그인 처리를 위한 SQL -->
	
	<select id="login" resultType="UserDTO" parameterType="UserDTO">
	SELECT id, nick, f_code FROM user where id =#{id} AND pw =#{pw} AND admin is null
	</select>
	
	<select id="adminLogin" parameterType="UserDTO" resultType="UserDTO">
	SELECT id, nick FROM user where id =#{id} AND pw =#{pw} AND admin is not null
	</select>
	
	<insert id="signup" parameterType="UserDTO">
		insert into user(id, f_code, pw, name, nick, birth, phone, email)
		values(#{id}, #{f_code}, #{pw}, #{name},#{nick}, #{birth}, #{phone}, #{email})
	</insert>
	
	<select id="idchk" parameterType="String" resultType="String">
		select id from user where id = #{id}
	</select>
	
	<select id="nickchk" parameterType="String" resultType="String">
	select nick from user where nick = #{nick}
	</select>
	
	<!-- sjs의 흔적 -->
	<!-- 회원정보 하나 가져오기 -->
	<select id="info" resultType="UserDTO" parameterType="Object">
		SELECT id,f_code,pw,name,nick,birth,phone,email
		from user
		where id = #{id}
	</select>
	<!-- 회원정보수정 -->
	<update id="edit" parameterType="map">
		update user set f_code = #{f_code}, pw = #{pw},nick = #{nick},birth = #{birth},phone = #{phone},email = #{email}
		where id = #{id}
	</update>
	
</mapper>