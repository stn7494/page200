<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ez.en.page.camping_rev_infoMapper">

	<!-- 캠핑장 예약정보 추가(admin) -->
	
	
	<!-- 캠핑장 예약정보 수정(admin) -->
	
	
	<!-- 캠핑장 예약정보 조회(해당 캠핑장 디테일 -> 예약하기에서 조회) -->
	<select id="select" resultType="CampingRevInfoDTO" parameterType="String">
		select * from camping_rev_info where cam_code=#{cam_code}
	</select>
	


</mapper>