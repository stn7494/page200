<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ez.en.page.themaMapper">

	<select id="selectOne" parameterType="String" resultType="ThemaDTO">
		select * from camping_thema where thema_code
	</select>

	<select id="list" resultType="ThemaDTO">
		select * FROM camping_thema
	</select>

</mapper>